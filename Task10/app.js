const text = document.querySelector(".block .text");

console.log(text);                                      // Весь блок с указанным классом
console.log(text.innerText);                            // Только текст